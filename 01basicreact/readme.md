## Installation Guide

```  npx create-react-app 01basicreact  ```

What this command does

- ``` npx ``` → runs the package without installing it globally.

- ``` create-react-app ``` → official React tool for setting up a new project.

- ``` 01basicreact ``` → the name of the new project folder.


👉 This sets up a ready-to-use React project using the CRA template. It automatically downloads and installs all the required dependencies (React, ReactDOM, testing libraries, build scripts) and a basic project structure.