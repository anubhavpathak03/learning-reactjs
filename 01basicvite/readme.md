## Installation Guide 

``` npm creat vite@latest ```

🔎 What this command does

- ``` npm ``` → Node Package Manager.

- ``` create ``` → runs a package initializer.

- ``` vite@latest ``` → fetches and runs the latest Vite project setup tool.

<br/>

> When combined, Vite handles the **bundling, hot reloading, and build optimization**, while React provides the **UI building blocks**.
